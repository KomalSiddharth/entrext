# 🎨 Visual Guide: Connect Your Supabase Account

## 📍 Where to Find Your Credentials

```

  Supabase Dashboard (https://supabase.com/dashboard)   │

                          ↓

  Select Your Project                                    │
  ┌─────────────────────────────────────────────────┐   │
  │  companion-landing                              │   │
  └─────────────────────────────────────────────────┘   │

                          ↓

  Settings ⚙️  (in left sidebar)                         │

                          ↓

  API Section                                            │
  ┌─────────────────────────────────────────────────┐   │
  │  Project URL                                    │   │
  │  https://xxxxx.supabase.co          [Copy]     │   │
  │                                                 │   │
  │  Project API keys                               │   │
  │  ┌─────────────────────────────────────────┐   │   │
  │  │ anon public (USE THIS ONE!)            │   │   │
  │  │ eyJhbGciOiJIUzI1NiIsInR5cCI6...  [Copy]│   │   │
  │  └─────────────────────────────────────────┘   │   │
  │  ┌─────────────────────────────────────────┐   │   │
  │  │ service_role (DON'T USE IN FRONTEND!)  │   │   │
  │  │ eyJhbGciOiJIUzI1NiIsInR5cCI6...       │   │   │
  │  └─────────────────────────────────────────┘   │   │
  └─────────────────────────────────────────────────┘   │

```

---

## 🔄 Complete Setup Flow

```

  STEP 1: Get Credentials from Supabase                       │
  ┌────────────────────────────────────────────────────────┐  │
  │  • Project URL: https://xxxxx.supabase.co             │  │
  │  • Anon Key: eyJhbGc...                               │  │
  └────────────────────────────────────────────────────────┘  │

                            ↓

  STEP 2: Update .env File                                    │
  ┌────────────────────────────────────────────────────────┐  │
  │  VITE_SUPABASE_URL=https://xxxxx.supabase.co          │  │
  │  VITE_SUPABASE_ANON_KEY=eyJhbGc...                    │  │
  └────────────────────────────────────────────────────────┘  │

                            ↓

  STEP 3: Create Database Table                               │
  ┌────────────────────────────────────────────────────────┐  │
  │  Go to SQL Editor → Run CREATE TABLE query            │  │
  │                                                        │  │
  │  CREATE TABLE waitlist (                              │  │
  │    id uuid PRIMARY KEY,                               │  │
  │    email text UNIQUE NOT NULL,                        │  │
  │    created_at timestamptz DEFAULT now()               │  │
  │  );                                                    │  │
  └────────────────────────────────────────────────────────┘  │

                            ↓

  STEP 4: Configure Permissions                               │
  ┌────────────────────────────────────────────────────────┐  │
  │  ALTER TABLE waitlist DISABLE ROW LEVEL SECURITY;     │  │
  └────────────────────────────────────────────────────────┘  │

                            ↓

  STEP 5: Test the Connection                                 │
  ┌────────────────────────────────────────────────────────┐  │
  │  Landing Page → Join Waitlist → Enter Email → Submit  │  │
  │                                                        │  │
  │  ✅ Success: "Welcome to the waitlist! 🌿"            │  │
  └────────────────────────────────────────────────────────┘  │

                            ↓

  STEP 6: Verify in Supabase                                  │
  ┌────────────────────────────────────────────────────────┐  │
  │  Table Editor → waitlist → See your test email        │  │
  └────────────────────────────────────────────────────────┘  │

```

---

## 🎯 Data Flow Diagram

```

  USER INTERACTION                                           │
                                                             │
  Landing Page                                               │
  ┌─────────────────────────────────────────────────────┐   │
  │  [Join the waitlist 💫]  ← User clicks button      │   │
  └─────────────────────────────────────────────────────┘   │
                          ↓                                  │
  ┌─────────────────────────────────────────────────────┐   │
  │  Dialog Opens                                       │   │
  │  ┌───────────────────────────────────────────────┐  │   │
  │  │  Email: [user@example.com]                   │  │   │
  │  │  [Submit]  ← User enters email & submits     │  │   │
  │  └───────────────────────────────────────────────┘  │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  FRONTEND PROCESSING                                        │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  1. Validate email format                           │   │
  │  2. Call addToWaitlist(email)                       │   │
  │  3. Show loading state                              │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  API LAYER (src/db/api.ts)                                  │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  export async function addToWaitlist(email) {       │   │
  │    const { data, error } = await supabase           │   │
  │      .from("waitlist")                              │   │
  │      .insert({ email })                             │   │
  │      .select()                                      │   │
  │      .maybeSingle();                                │   │
  │    return data;                                     │   │
  │  }                                                  │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  SUPABASE CLIENT (src/db/supabase.ts)                       │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  const supabaseUrl = VITE_SUPABASE_URL              │   │
  │  const supabaseKey = VITE_SUPABASE_ANON_KEY         │   │
  │  export const supabase = createClient(url, key)     │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  SUPABASE DATABASE                                          │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  Table: waitlist                                    │   │
  │  ┌───────────────────────────────────────────────┐  │   │
  │  │ id    │ email              │ created_at      │  │   │
  │  ├───────┼────────────────────┼─────────────────┤  │   │
  │  │ uuid  │ user@example.com   │ 2025-11-19...  │  │   │
  │  └───────┴────────────────────┴─────────────────┘  │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  RESPONSE TO USER                                           │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  ✅ Success Toast                                   │   │
  │  "Welcome to the waitlist! 🌿"                      │   │
  │  "We'll notify you when Companion launches."        │   │
  └─────────────────────────────────────────────────────┘   │

```

---

## 🔐 Security Architecture

```

  FRONTEND (Public)                                          │
  ┌─────────────────────────────────────────────────────┐   │
  │  • Uses ANON KEY (safe to expose)                   │   │
  │  • Limited permissions                              │   │
  │  • Can only INSERT into waitlist                    │   │
  │  • Cannot delete or modify existing data            │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  SUPABASE API LAYER                                         │
  ┌─────────────────────────────────────────────────────┐   │
  │  • Validates API key                                │   │
  │  • Checks permissions                               │   │
  │  • Enforces rate limits                             │   │
  │  • Prevents SQL injection                           │   │
  └─────────────────────────────────────────────────────┘   │

                          ↓

  DATABASE (Protected)                                       │
  ┌─────────────────────────────────────────────────────┐   │
  │  • RLS disabled for public waitlist                 │   │
  │  • Unique constraint prevents duplicates            │   │
  │  • Encrypted at rest                                │   │
  │  • Automatic backups                                │   │
  └─────────────────────────────────────────────────────┘   │

```

---

## 📊 File Structure

```
companion-landing/

 .env                          ← UPDATE THIS!
   ├── VITE_SUPABASE_URL        ← Your project URL
   └── VITE_SUPABASE_ANON_KEY   ← Your anon key

 src/
   ├── db/
   │   ├── supabase.ts          ← Supabase client (reads .env)
   │   └── api.ts               ← API functions (uses client)
   │
   ├── pages/
   │   └── LandingPage.tsx      ← Waitlist form (calls API)
   │
   └── types/
       └── types.ts             ← TypeScript types

 supabase/
    └── migrations/
        └── 01_create_waitlist_table.sql  ← SQL to run in Supabase
```

---

## 🎨 Supabase Dashboard Navigation

```
Supabase Dashboard
 🏠 Home
 📊 Table Editor          ← View your waitlist data here
   └── waitlist table
 🔍 SQL Editor            ← Run CREATE TABLE query here
 🔐 Authentication
 📦 Storage
 🌐 Edge Functions
 ⚙️  Settings             ← Get credentials here
    └── API
        ├── Project URL
        └── API Keys
            ├── anon public   ← USE THIS
            └── service_role  ← DON'T USE IN FRONTEND
```

---

## ✅ Verification Checklist

```
Setup Progress:

 Step 1: Created Supabase account
 Step 2: Created new project
 Step 3: Copied Project URL
 Step 4: Copied anon public key
 Step 5: Updated .env file
 Step 6: Created waitlist table
 Step 7: Created indexes
 Step 8: Disabled RLS
 Step 9: Tested form submission
 Step 10: Verified data in Table Editor

 All done? You're ready to collect emails!
```

---

## 🚨 Common Mistakes to Avoid

```
 WRONG                          ✅ CORRECT

Using service_role key            Use anon public key
in frontend                       in frontend

Forgetting to create table        Run CREATE TABLE SQL
                                  in SQL Editor

Leaving RLS enabled               Disable RLS or create
without policies                  insert policy

Not restarting dev server         Restart after changing
after .env changes                .env file

Committing .env to Git            Add .env to .gitignore
```

---

## 📈 Success Indicators

```
 Everything is working when you see:

1. Form Submission
   Landing Page → Click "Join waitlist" → Enter email → Submit
   ↓
   ✅ "Welcome to the waitlist! 🌿"

2. Database Verification
   Supabase Dashboard → Table Editor → waitlist
   ↓
   ✅ Your test email appears in the table

3. Duplicate Prevention
   Submit same email again
   ↓
   ⚠️ "Already on the list!"

4. Error Handling
   Submit invalid email
   ↓
   ❌ Validation error shows
```

---

## 🎉 You're All Set!

```

                                                             │
              🎊 CONGRATULATIONS! 🎊                         │
                                                             │
     Your Companion landing page is now connected to         │
              your own Supabase account!                     │
                                                             │
  ┌─────────────────────────────────────────────────────┐   │
  │  ✅ Database: Connected                             │   │
  │  ✅ Table: Created                                  │   │
  │  ✅ Permissions: Configured                         │   │
  │  ✅ Form: Working                                   │   │
  │  ✅ Ready: To collect emails!                       │   │
  └─────────────────────────────────────────────────────┘   │
                                                             │
              Start collecting waitlist signups! 🚀          │
                                                             │

```

---

**Need help?** Check the detailed guide in `SUPABASE_CUSTOM_SETUP.md`

**Quick reference?** See `QUICK_SETUP_GUIDE.md`
